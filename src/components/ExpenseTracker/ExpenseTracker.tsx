import { z } from "zod";

const schema = {};

interface ExpenseTracker {}

const ExpenseTracker = () => {
  return (
    <div>
      <p>Expense Tracker</p>
    </div>
  );
};

export default ExpenseTracker;
